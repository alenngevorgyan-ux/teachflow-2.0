You are extracting learning outcomes from an official curriculum document (standard or subject program) for Grade {{grade}}, subject "{{subject}}".

DOCUMENT TEXT:
{{text}}

RULES (strict):
- Only extract outcomes that are written in the document text above. Do not add, infer or complete outcomes.
- "code": copy the outcome code EXACTLY as it is written in the document (same letters, digits and separators). If the document gives no explicit code for an outcome, set "code" to null. NEVER invent, number or construct a code yourself.
- "text": copy the outcome description VERBATIM from the document, character for character (the same Armenian spelling and punctuation). Do not paraphrase, shorten, translate or fix it.
- "grade": {{grade}}.
- If the document contains no outcomes, return an empty list.

Return structured JSON according to the schema.
